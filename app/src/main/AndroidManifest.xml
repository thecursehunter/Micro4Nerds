<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Micro4Nerds">

        <!-- Màn hình chính, điểm khởi đầu của ứng dụng -->
        <activity
            android:name=".ui.user.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các màn hình xác thực -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <!-- Các màn hình chức năng người dùng -->
        <activity
            android:name=".ui.user.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.CartActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.ProductDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.CheckoutActivity"
            android:exported="false" />

    </application>

</manifest>